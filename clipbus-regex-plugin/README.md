# Regex Tester Plugin

Clipbus 插件，提供正则表达式实时测试功能。

## 功能

- 输入正则表达式 + 标志位（`g` / `i` / `m` 等），对任意文本即时匹配
- 实时显示匹配数、每条匹配的原文、位置索引及捕获组
- 非法正则立即提示错误信息，不崩溃
- 自动以剪贴板文本预填"测试文本"输入框
- 点击"复制匹配"将所有匹配项换行连接后写回剪贴板
- 最多返回 200 条匹配；文本超过 20,000 字符自动截断，避免界面卡顿
